# Base image for ubuntu 16.04 Xenial LTS
FROM ubuntu:xenial-20180228

# Allow default node version to be overriden by an ARG
ARG NODE_VERSION=8.10.0
ARG YARN_VERSION=1.5.1

# Persist node version into an env var
ENV NODE_VERSION ${NODE_VERSION}
ENV YARN_VERSION ${YARN_VERSION}

# disables interactive shell and allows software licenses to be autoaccepted
# via apt-get install / update
ENV DEBIAN_FRONTEND noninteractive

# Update / Upgrade existing system components
# this should patch most security vulnerabilities
RUN apt-get update && apt-get upgrade -y && apt-get install -y \
    curl \
    git \
    libssl-dev \
    libffi-dev \
    python \
    build-essential

# Install node.js sourced from
# https://github.com/nodejs/docker-node/blob/ab12e9bab43bde5efd2e9fa278d941e4062c6f4a/8/wheezy/Dockerfile
# ignore the docker instruction that sets a default user and group for node
# it's not even used in the remote dockerfile it's only created

# gpg keys listed at https://github.com/nodejs/node#release-team
RUN set -ex \
  && for key in \
    94AE36675C464D64BAFA68DD7434390BDBE9B9C5 \
    FD3A5288F042B6850C66B31F09FE44734EB7990E \
    71DCFD284A79C3B38668286BC97EC7A07EDE3FC1 \
    DD8F2338BAE7501E3DD5AC78C273792F7D83545D \
    C4F0DFFF4E8C1A8236409D08E73BC641CC11F4C8 \
    B9AE9905FFD7803F25714661B63B535A4C206CA9 \
    56730D5401028683275BD23C23EFEFE93C4CFFFE \
    77984A986EBC2AA786BC0F66B01FBB92821C587A \
  ; do \
    gpg --keyserver hkp://p80.pool.sks-keyservers.net:80 --recv-keys "$key" || \
    gpg --keyserver hkp://ipv4.pool.sks-keyservers.net --recv-keys "$key" || \
    gpg --keyserver hkp://pgp.mit.edu:80 --recv-keys "$key" ; \
  done

RUN ARCH= && dpkgArch="$(dpkg --print-architecture)" \
  && case "${dpkgArch##*-}" in \
    amd64) ARCH='x64';; \
    ppc64el) ARCH='ppc64le';; \
    *) echo "unsupported architecture"; exit 1 ;; \
  esac \
  && curl -SLO "https://nodejs.org/dist/v$NODE_VERSION/node-v$NODE_VERSION-linux-$ARCH.tar.xz" \
  && curl -SLO --compressed "https://nodejs.org/dist/v$NODE_VERSION/SHASUMS256.txt.asc" \
  && gpg --batch --decrypt --output SHASUMS256.txt SHASUMS256.txt.asc \
  && grep " node-v$NODE_VERSION-linux-$ARCH.tar.xz\$" SHASUMS256.txt | sha256sum -c - \
  && tar -xJf "node-v$NODE_VERSION-linux-$ARCH.tar.xz" -C /usr/local --strip-components=1 --no-same-owner \
  && rm "node-v$NODE_VERSION-linux-$ARCH.tar.xz" SHASUMS256.txt.asc SHASUMS256.txt \
  && ln -s /usr/local/bin/node /usr/local/bin/nodejs


RUN set -ex \
  && for key in \
    6A010C5166006599AA17F08146C2130DFD2497F5 \
  ; do \
    gpg --keyserver hkp://p80.pool.sks-keyservers.net:80 --recv-keys "$key" || \
    gpg --keyserver hkp://ipv4.pool.sks-keyservers.net --recv-keys "$key" || \
    gpg --keyserver hkp://pgp.mit.edu:80 --recv-keys "$key" ; \
  done \
  && curl -fSLO --compressed "https://yarnpkg.com/downloads/$YARN_VERSION/yarn-v$YARN_VERSION.tar.gz" \
  && curl -fSLO --compressed "https://yarnpkg.com/downloads/$YARN_VERSION/yarn-v$YARN_VERSION.tar.gz.asc" \
  && gpg --batch --verify yarn-v$YARN_VERSION.tar.gz.asc yarn-v$YARN_VERSION.tar.gz \
  && mkdir -p /opt \
  && tar -xzf yarn-v$YARN_VERSION.tar.gz -C /opt/ \
  && ln -s /opt/yarn-v$YARN_VERSION/bin/yarn /usr/local/bin/yarn \
  && ln -s /opt/yarn-v$YARN_VERSION/bin/yarnpkg /usr/local/bin/yarnpkg \
  && rm yarn-v$YARN_VERSION.tar.gz.asc yarn-v$YARN_VERSION.tar.gz

# several vulnerabilities exist within kerboros and this package is unneeded
RUN apt-get purge -y krb5-kdc krb5-admin-server krb5-config krb5-locales krb5-user krb5.conf
RUN rm -rf /var/lib/krb5kdc

# vulnerabilities exist within firefox which is by default installed on ubunut xenial
RUN apt-get purge firefox

# Clean up upgrade and install to make the image smaller
# keep this at the bottom because once run you need to run apt-get update again
RUN rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*

CMD [ "node" ]
